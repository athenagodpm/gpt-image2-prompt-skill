---
name: gpt-image2-prompt
description: 根据用户自然语言描述，生成符合 GPT Image 2 标准框架的结构化生图 prompt。核心机制：三段落框架（主体/场景/辅助元素 → 风格/光影/构图 → 弱化补丁）+ 降噪方法论，避免脏图。触发词：生图、AI 绘图、gpt-image、生成图片、画图、做图、海报插图、配图。使用场景：需要输出 gpt-image-2（通过 fal.ai 调用）的 prompt 时。
---

# GPT Image 2 Prompt 生成器

根据用户对图片内容的需求，输出符合 GPT Image 2 标准框架的结构化 prompt。

## 核心原则

**GPT Image 2 最大的坑：它太"听话"了。** prompt 信息越多，越想把所有细节画到位，导致"脏图"（边缘发糊、暗部噪点、头发结块、整体灰蒙蒙）。

**解决方案：做减法 + 弱化补丁**
- 控制信息量：1 主体 + 1 场景 + 1-2 辅助元素
- 末尾加弱化补丁（降噪的关键）
- 避免"超高细节/ultra detailed/masterpiece"等脏词

## 工作流

### Step 1: 解析用户需求

从用户描述中提取 6 个要素：

| 要素 | 说明 | 约束 |
|---|---|---|
| 主体 | 视觉核心 | 最多 2 个 |
| 核心场景 | 在哪里、什么氛围 | 一句话 |
| 辅助元素 | 最有必要的点缀 | 1-2 个 |
| 风格 | 整体视觉调性 | 只选 1 个 |
| 光影 | 光线方向/性质 | 选柔和类 |
| 构图 | 画面布局 | 简洁明确 |

### Step 2: 做减法（核心）

用精简准则过滤用户原始描述中的冗余信息：

- **1 主体** — 多余的删掉。如果用户说了多个，挑最重要的
- **1 场景** — 用户说"海边+雪山+都市"？只保留最核心的一个
- **1-2 辅助元素** — 不是购物清单，删到只剩最有氛围感的
- **1 风格** — "赛博朋克+水墨+浮世绘"？只保留一个
- **1 光源** — "阳光+霓虹+烛光"同时出现？只留主光源
- **检查矛盾** — "雨天"+ "蓝天白云"不成立，"体积光"+ "阴天"不成立

### Step 3: 替换脏词

扫描用户描述，替换这些 GPT Image 2 脏词：

| 原始词（避免） | 替换为 |
|---|---|
| 超高细节、极致写实、毛孔可见、每一处纹理、超现实细节 | 弱化细节、干净简洁、克制刻画 |
| ultra detailed, masterpiece, best quality, 8k, photorealistic | （直接删除） |
| 戏剧性强光、体积光 | 柔和漫反射、散射光 |
| 强对比、高对比 | 柔和色调、低饱和 |

### Step 4: 套用标准框架

按三段式结构组织：

```
[主体] — [核心场景] — [1-2个辅助元素]。
[风格/氛围] — [光影] — [构图]。
[弱化补丁]
```

### Step 5: 选择弱化补丁

根据风格从以下选择匹配的弱化补丁：

| 风格 | 弱化补丁 |
|---|---|
| 通用/写实 | "整体画面弱化细节，干净高级感，柔和光影，控制噪点" |
| 极简/留白 | "极简构图，大量留白，笔触统一，抑制过曝和颗粒感" |
| 极繁 | "细节丰富但渲染干净，密集但有层次，控制噪点" |
| 动漫 | "干净的线条，无噪点无颗粒，画面纯净" |
| 产品/电商 | "干净的产品质感，柔和光影，无噪点" |

### Step 6: 输出最终 prompt

输出格式：**直接将三段式 prompt 呈现给用户**，附带一句简短说明。

需要更详细的词库和迭代工作流时，读取 `references/prompt-framework.md`。

## 示例

### 示例 1：赛博朋克少女雨夜街头

**用户输入：**
> 我想要一张赛博朋克风格的图，一个黑衣少女站在雨夜的街头，周围有霓虹灯和全息广告，背景是未来城市，整体氛围要酷一点

**输出：**

```
一位黑衣少女站在雨夜的霓虹街头，
身旁只有无声的全息广告在闪烁。
赛博朋克美学，克制风格，
柔和漫反射霓虹光，冷色调为主，
三分法构图，背景虚化。

整体画面弱化细节，干净高级感，
柔和光影，控制噪点，电影感色彩分级。
```

### 示例 2：智能手表电商产品图

**用户输入：**
> 帮我做一个智能手表的电商产品图，手表是金属表盘搭配橙色硅胶表带，要看起来很高档

**输出：**

```
一块圆形金属表盘的智能手表，
搭配橙色硅胶表带，悬浮在柔和渐变背景中。
高级产品摄影风格，极简调性，
柔光箱布光，漫反射为主，
居中构图，大量留白。

干净的产品质感，柔和光影，无噪点，
均匀布光，控制反光。
```

### 示例 3：AI 科普文章封面

**用户输入：**
> 一张关于 AI 工具科普文章的封面海报，需要包含文字"2026 AI 工具指南"

**输出：**

```
极简几何背景，一个发光的 AI 芯片图标居中，
周围环绕抽象的数据流线条。
科技感现代风格，干净利落，
均匀柔光，低饱和冷色调，
居中构图，大量留白。
文字："2026 AI 工具指南"

整体画面弱化细节，干净高级感，
统一笔触，抑制过曝，控制噪点。
```

## 深入参考

- **词库 & 迭代工作流**: 读取 `references/prompt-framework.md`
